{% if module.type.handle == 'form' %}

    <div class="container relative">
        <div class="md:max-w-3xl mx-auto richtext">
            {{ module.form.render() }}
        </div>
    </div>

{% endif %}